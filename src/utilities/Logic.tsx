const isZero = (value: number) => value === 0;
const lengthIsZero = (value: Array<any>) => value.length === 0;
const notZero = (value: number) => value !== 0;

export { isZero, notZero, lengthIsZero };
